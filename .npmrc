# Hoist Prisma and Neon packages so Wrangler's esbuild bundler can resolve them
# from any workspace package without needing them in every package.json
public-hoist-pattern[]=*prisma*
public-hoist-pattern[]=@prisma*
public-hoist-pattern[]=@neondatabase*
